<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MiningTradingMobileApp.ViewModels"
             xmlns:models="clr-namespace:MiningTradingMobileApp.Models"
             x:Class="MiningTradingMobileApp.Views.BuyerPages.TradeList"
             x:DataType="viewmodels:TradeListViewModel">
    <ContentView.BindingContext>
        <viewmodels:TradeListViewModel />
    </ContentView.BindingContext>
    <VerticalStackLayout Spacing="10">
        <Label Text="Your Trades" FontSize="Large" FontAttributes="Bold" />
        <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />
        <CollectionView ItemsSource="{Binding Trades}" IsVisible="{!Binding IsLoading}">
            <CollectionView.EmptyView>
                <Label Text="No trades found." HorizontalOptions="Center" VerticalOptions="Center" />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Trade">
                    <Frame CornerRadius="5" Margin="5" Padding="10" HasShadow="True">
                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding TradeNumber, StringFormat='Trade #{0}'}" FontAttributes="Bold" />
                                <Label Text="{Binding MetalType, StringFormat='Metal Type: {0}'}" />
                                <Label Text="{Binding Quantity, StringFormat='Quantity: {0} tons'}" />
                                <Label Text="{Binding TotalValue, StringFormat='Total Value: {0:C}'}" />
                                <Label Text="{Binding Status, StringFormat='Status: {0}'}" />
                            </VerticalStackLayout>
                            <Button Grid.Column="1" Text="View Details" Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:TradeListViewModel}}, Path=ViewTradeDetailsCommand}" CommandParameter="{Binding TradeNumber}" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentView>