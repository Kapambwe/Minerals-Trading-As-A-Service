<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MiningTradingMobileApp.ViewModels"
             xmlns:models="clr-namespace:MiningTradingMobileApp.Models"
             x:Class="MiningTradingMobileApp.Views.SellerPages.SettlementList"
             x:DataType="viewmodels:SettlementListViewModel">
    <ContentView.BindingContext>
        <viewmodels:SettlementListViewModel />
    </ContentView.BindingContext>
    <VerticalStackLayout Spacing="10">
        <Label Text="Your Settlements" FontSize="Large" FontAttributes="Bold" />
        <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />
        <CollectionView ItemsSource="{Binding Settlements}" IsVisible="{!Binding IsLoading}">
            <CollectionView.EmptyView>
                <Label Text="No settlements found." HorizontalOptions="Center" VerticalOptions="Center" />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Settlement">
                    <Frame CornerRadius="5" Margin="5" Padding="10" HasShadow="True">
                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding SettlementDate, StringFormat='Settlement Date: {0:d}'}" FontAttributes="Bold" />
                                <Label Text="{Binding TradeId, StringFormat='Trade ID: {0}'}" />
                                <Label Text="{Binding SettlementAmount, StringFormat='Amount: {0:C}'}" />
                                <Label Text="{Binding SettlementType, StringFormat='Type: {0}'}" />
                                <Label Text="{Binding Status, StringFormat='Status: {0}'}" />
                            </VerticalStackLayout>
                            <Button Grid.Column="1" Text="View Details" Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SettlementListViewModel}}, Path=ViewSettlementDetailsCommand}" CommandParameter="{Binding Id}" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentView>